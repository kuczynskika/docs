---
title: "Zapier integration"
description: "Connect MessageDesk to thousands of apps using Zapier to automate your messaging workflows."
sidebarTitle: "Overview"
---

# Zapier integration

Use the MessageDesk Zapier integration to connect your messaging workflows with thousands of other apps. Automate tasks like syncing contacts, sending messages based on external events, and triggering actions in other tools when messages are received.

---

## What is Zapier?

Zapier is an automation platform that connects different apps together. Each automation is called a **Zap** and consists of:

- **Trigger:** The event that starts the Zap
- **Action:** What happens when the trigger fires

---

## Available triggers

MessageDesk provides these triggers for your Zaps:

| Trigger | Description |
|---------|-------------|
| Message received | Fires when a new inbound message arrives in your inbox |
| New workspace | Fires when a new workspace is created |
| Message sent | Fires when an outbound message is sent from MessageDesk |

---

## Available actions

Use these actions to control MessageDesk from other apps:

| Action | Description |
|--------|-------------|
| Create or update contact | Add a new contact or update an existing one based on phone number |
| Send message | Send an SMS message to a contact |

---

## Getting started

1. Log in to your [Zapier account](https://zapier.com)
2. Search for **MessageDesk** in the app directory
3. Connect your MessageDesk account when prompted
4. Build your first Zap using the triggers and actions above

---

## Common use cases

- **CRM sync:** Automatically create contacts in MessageDesk when new leads are added to your CRM
- **Form responses:** Send a welcome message when someone submits a form
- **Appointment reminders:** Trigger messages from your scheduling tool
- **Lead notifications:** Get notified in Slack when a new message arrives
