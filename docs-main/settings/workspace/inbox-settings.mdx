---
title: "Inbox Settings (Labels, Message Templates & Relays)"
description: "Set workspace-wide appearance, labels, and message templates for every user in your MessageDesk inbox."
sidebarTitle: "Inbox settings"
og:title: "Workspace Inbox Settings - MessageDesk"
og:description: "Set workspace-wide appearance, labels, and message templates for every user in your MessageDesk inbox."
og:image: "https://uploads-ssl.webflow.com/6349fd438ecb3a3594605225/63dd3a9bd4c06c39183e7b60_messagedesk-og-1200x630.webp"
---

# Learn More About Label, Template & Relay Inbox Settings for Your Entire Workspace

Use **Inbox Settings** to manage **workspace-wide defaults** for **Labels** and **Templates**. These settings give your team a consistent, organized inbox experience.

<Note>
  **Note:** Workspace settings act as **defaults**. **User preferences always override** workspace defaults and are **not** overwritten by later workspace changes.
</Note>

---

## Workspace Labels (Create, Edit, Remove)

Standardize how your team groups and filters conversations with shared Labels.

**Where to go:**\
**Account → Settings → Workspace Settings → Labels**

**Actions:**

- **Create label** – define **name**, **color**, and **icon**
- **Search / manage** existing labels
- **Edit** label properties (name, color, icon)
- **Remove (delete)** labels no longer used

<Tip>
  **Tips:**

  - Keep names short and scannable (e.g., `Support: Urgent`, `HR: New Hire`, `Region: West`).
  - Use consistent icon/color patterns by function or department.
</Tip>

<Frame>
  ![Workspace Labels Settings](/images/workspace-labels-settings.png)
</Frame>

---

## Workspace Message Templates (Shared Message Templates)

Publish pre-approved, reusable messages for your whole team.

**Where to go:**\
**Account → Settings → Workspace Settings → Templates**

**Actions:**

- **Create template** – add a clear **name** and **message content**
- **Search / manage** existing templates
- **Edit** template name/content

<Note>
  **Note:**

  - Templates support **media**, **emoji**, and **personalization tags** (e.g., `{{ ContactFirstName }}`, `{{ WorkspaceName }}`).
  - Keep names descriptive so teammates can find the right template fast (e.g., “Appt Reminder – 24h”).
  - For personal/one-off templates, users can save their own in **My Settings** (if enabled).
</Note>

<Frame>
  ![Workspace Message Templates Settings](/images/workspace-message-templates-settings.png)
</Frame>

---

## Workspace Relay Settings

You can manage Relays for your entire workspace from the **Relays module** or from **Workspace Settings**.

**Where to go:**\
**Account → Settings → Workspace Settings → Relays**

Currently, you can:

- Update a Relay's status (active vs. inactive).
- Edit a Relay (triggers, conditions, and actions).
- Delete a Relay

<Frame>
  ![Workspace Settings Relays](/images/workspace-settings-relays.png)
</Frame>

## Permissions & Best Practices

**Who can manage workspace settings?**\
Admins (and Managers, if allowed by your [**Roles & Permissions**](/settings/workspace/team-management)) can update the workspace's Inbox Settings.

**Recommended setup:**

- Define a small, clear label taxonomy before scaling usage.
- Review labels quarterly to remove duplicates or stale tags.
- Keep template copy concise and compliant (include opt-out language where appropriate).
- Use workspace appearance to set a professional default; let users personalize if needed.