---
title: "Relay templates"
description: "All 58 MessageDesk Relay templates that ship in the app, the builds behind them organized by trigger, and an industry-by-industry starting point."
sidebarTitle: "Templates"
og:title: "Relay templates - Ready-Made Automation Templates for MessageDesk"
og:description: "All 58 Relay templates that ship in the app, plus the builds behind them organized by trigger."
og:image: "https://uploads-ssl.webflow.com/6349fd438ecb3a3594605225/63dd3a9bd4c06c39183e7b60_messagedesk-og-1200x630.webp"
---

# Relay templates

A library of ready-made Relay templates. Start from a template that ships in the app, or build from the outcome-first library below. Each library entry lists the **trigger**, any **conditions**, the **actions**, and a brief **why this works** note.

<Note>
  **Every template here is a starting point.** Adjust the wording, labels, assignees, active hours, and conditions to match your team's workflow. For step-by-step instructions on building a Relay from scratch, including how to use **AND/OR** between conditions, chain actions, and add delays, see [Create and edit a Relay](/relays/create-edit). For a full reference of the available [triggers, conditions, and actions](/relays/building-blocks), see the Relay building blocks page.
</Note>

<Tip>
  **Stacking templates on the same Trigger?** You can build more than one Relay on the same trigger. For example, an after-hours reply and a keyword reply both on **Message Received**. Set the **Order** on the Relays list so the more specific Relay runs first. All matching Relays run in Order. See [Order your Relays](/relays/create-edit#1a-order-your-relays).
</Tip>

![Relay Templates 1](/images/relay-templates-1.png "Relay Templates 1")

---

## Starter templates that ship today

Seven outcome groups, 58 templates. Pick one, name it, and adjust the fields. Every template arrives **paused**, so nothing runs until you toggle it **On**.

<Tip>
  **Outcome groups expand and collapse.** The board opens with the outcome headings collapsed so you can scan all seven at once, then expand only the group you need. Individual template cards expand and collapse too, so you can read a template's detail without leaving the board.
</Tip>

Each group below lists every shipped template and what it does. Where this page documents the underlying build, the third column links to it, so you can either install the template or build the same outcome by hand.

### Assign & Route Conversations

| Template | What it does | Build reference |
| --- | --- | --- |
| **Assign to Teammate** | Assigns new conversations to a teammate you pick | [Auto-assign](#4-phone-line-routing) |
| **Auto Assign to First Responder** | Assigns the conversation to the first teammate who replies | [Whoever answers a lead first owns the follow-up](#whoever-answers-a-lead-first-owns-the-follow-up) |
| **Unassign on Close** | Clears every assignee when a conversation closes | [Reset assignees on close](#clean-reset-and-system-sync-every-time-a-thread-closes) |
| **Route by Phone Line** | Assigns and labels based on the connected number that received the message | [Phone line routing](#4-phone-line-routing) |
| **Route by Keyword** | Assigns the thread to the team that owns a texted keyword | [Keyword-based routing](#5-keyword-based-routing) |
| **Escalate Urgent Texts** | Flags and routes messages that contain urgent wording | [Safety and incident texts escalate the second they land](#5-keyword-based-routing) |
| **Escalate Complaints and Cancellations** | Routes churn-risk language to an owner who can save the account | None yet |
| **Label Applied → Assign Owner** | Turns applying a label into an assignment | None yet |
| **Flag Unclaimed Conversations** | Labels threads that no one has picked up | [Subcontractor and supplier texts never sit unclaimed](#subcontractor-and-supplier-texts-never-sit-unclaimed) |
| **Re-triage Reopened Conversations** | Clears assignees and labels the thread `Reopened` when a closed conversation comes back | [Re-triage on reopen](#9-re-triage-on-reopen) |
| **Route Inbound Calls** | Labels, assigns, and drops a note on an inbound call | [Log and route inbound calls to the right owner](#log-and-route-inbound-calls-to-the-right-owner) |
| **Comment Commands (#escalate)** | Watches internal comments for a command word like `#escalate` and routes the thread | None yet |
| **Route a Label Namespace** | Matches every label under one prefix (for example `Region:`) with a single rule | [One rule that routes an entire namespace of labels](#one-rule-that-routes-an-entire-namespace-of-labels) |
| **Route by Contact Property — Part 1 (Bridge)** | Writes the deciding contact property into a comment, so a conversation Relay can act on it | [Bridge a contact property](#bridge-a-contact-property-to-a-label-and-an-owner) |
| **Route by Contact Property — Part 2 (Apply Label)** | Reads that comment, then labels the thread and assigns its owner | [Bridge a contact property](#bridge-a-contact-property-to-a-label-and-an-owner) |

<Note>
  **Route by Contact Property takes both halves.** Contact triggers fire against a contact, not a conversation, so they can't label or assign on their own. Part 1 writes the property value into a comment; Part 2 reads that comment and does the labeling and assignment. Install both, or neither works. Part 2 also needs one copy per possible value. See [Bridge a contact property to a label and an owner](#bridge-a-contact-property-to-a-label-and-an-owner) for the full build.
</Note>

### Organize Conversations & Update Status

| Template | What it does | Build reference |
| --- | --- | --- |
| **Identify Unknown Contacts** | Asks anonymous contacts for their name | [First-touch reply for anonymous contacts](#2-first-touch-reply-for-anonymous-contacts) |
| **Label Conversation** | Auto-labels new conversations | None yet |
| **Auto Close No Reply Needed** | Closes threads labeled `No Reply Needed` and clears the label when a reply arrives | [Auto-close conversations that don't need a reply](#auto-close-conversations-that-dont-need-a-reply) |
| **Label After-Hours Texts** | Labels messages that land outside your operating hours | [Off-duty driver messages still get an instant acknowledgement](#out-of-office-for-one-teammate-even-on-a-shared-line) |
| **Label by Phone Line** | Labels the thread by the connected number that received the message | [Phone line routing](#4-phone-line-routing) |
| **Auto-Close on Confirmation Keyword** | Closes the thread when the contact texts a confirmation word | [Auto-close after acknowledgement](#13-auto-close-after-acknowledgment) |
| **Handoff Note on Assignment** | Posts an internal comment with context for the new owner | [Leave an automatic handoff note for the team](#leave-an-automatic-handoff-note-for-the-team) |
| **Resolved Label on Close** | Adds a `Resolved` label when a conversation closes | [Clean reset and system sync every time a thread closes](#clean-reset-and-system-sync-every-time-a-thread-closes) |
| **Keep Keyword Threads Unread** | Forces matching threads back to **Unread** so a teammate still reviews them | [After-hours auto-reply](#out-of-office-for-one-teammate-even-on-a-shared-line) |
| **Reopen on Label Removed** | Reopens the conversation when a label comes off | [When a label comes off, sync the change](#when-a-label-comes-off-sync-the-change) |

### Respond to Messages

| Template | What it does | Build reference |
| --- | --- | --- |
| **Out-of-Office (Away) Autoresponse** | Texts an autoresponse outside operating hours | [Out-of-office for one teammate, even on a shared line](#out-of-office-for-one-teammate-even-on-a-shared-line) |
| **After-Hours Auto Reply** | Texts an autoresponse outside hours and keeps the thread queued for morning review | [After-hours auto-reply](#out-of-office-for-one-teammate-even-on-a-shared-line) |
| **Respond to a Keyword** | Auto-replies when a contact texts a keyword | [Keyword-based routing](#5-keyword-based-routing) |
| **Instant Acknowledgement (New Conversations)** | Replies to the first message on a brand-new thread | [Website-to-text intake lands in a shared, trackable inbox](#2-first-touch-reply-for-anonymous-contacts) |
| **Send a Link on Keyword** | Texts a link when a contact texts the matching keyword | [Applicant first-touch that never goes cold](#5-keyword-based-routing) |
| **Delayed Follow-Up Reply** | Sends a follow-up reply after a delay you set | None yet |
| **Keyword Menu — Option 1 / 2 / 3** | A texted menu where each option gets its own reply and owner | [A texted keyword menu that routes itself](#5-keyword-based-routing) |
| **Broadcast Reply Tracker (ACK)** | Labels and logs every acknowledgement reply to a broadcast | [Policy and route acknowledgements get tracked, not chased](#5-keyword-based-routing) |

<Note>
  **Add your own opt-out language.** Templates that send a message ship with placeholder copy. Keep "Reply STOP to opt out." in any automated outbound text so you stay aligned with carrier requirements.
</Note>

### Start Conversations from Contacts & Other Tools

| Template | What it does | Build reference |
| --- | --- | --- |
| **Welcome New Contacts** | Texts a welcome the moment a contact is saved | None yet |
| **Text When a Contact Property Changes** | Texts a contact when a property flips to a value you pick | None yet |
| **Text from Another Tool (Inbound Webhook)** | Lets an outside system text a contact through MessageDesk | [Turn an event in another tool into a text](#turn-an-event-in-another-tool-into-a-text) |
| **Appointment or Order Update from Your System** | Texts an appointment or order update built from a webhook payload | [Turn an event in another tool into a text](#turn-an-event-in-another-tool-into-a-text) |
| **Log External Event as a Comment** | Records an outside event as an internal comment instead of a text | None yet |
| **IVR Caller → Menu Link (Voice-to-Text)** | Texts a menu link to someone who calls in | [Log and route inbound calls to the right owner](#log-and-route-inbound-calls-to-the-right-owner) |

### Keep Contacts in Sync

| Template | What it does | Build reference |
| --- | --- | --- |
| **Sync a Contact Property from Another System** | Updates a contact property from a webhook payload | [Keep a contact property in sync from an outside system](#keep-a-contact-property-in-sync-from-an-outside-system) |
| **Push New Contacts to Your CRM** | Sends every newly saved contact to your endpoint | None yet |
| **Push Contact Changes to Your CRM** | Sends contact property changes to your endpoint | None yet |

### Connect to Other Systems

| Template | What it does | Build reference |
| --- | --- | --- |
| **Send Webhook** | A webhook starter you point at your own endpoint | [Send Webhook](/relays/webhooks#outbound-webhooks) |
| **Push Inbound Messages to Your System** | Streams inbound texts to your endpoint | [Push new inbound message to an external system](#10-push-new-inbound-message-to-an-external-system) |
| **Notify Slack/Teams on New Message** | Pings your chat tool when a message arrives | [Push new inbound message to an external system](#10-push-new-inbound-message-to-an-external-system) |
| **Sync Conversation Close to Your CRM** | Pushes the close event downstream | [Notify an external system on conversation close](#clean-reset-and-system-sync-every-time-a-thread-closes) |
| **Forward Internal Comments** | Sends internal comments to your endpoint | None yet |
| **Label Applied → Update CRM Stage** | Turns applying a label into a downstream stage change | None yet |
| **Label Removed → Sync the Change** | Pushes label removals downstream | [When a label comes off, sync the change](#when-a-label-comes-off-sync-the-change) |
| **Notify on Assignment** | Pings your notifier when a conversation gets an owner | [Notify the new assignee on assignment](#leave-an-automatic-handoff-note-for-the-team) |
| **Log Inbound Calls to Your System** | Sends inbound call events to your endpoint | [Log and route inbound calls to the right owner](#log-and-route-inbound-calls-to-the-right-owner) |

<Note>
  **Webhook templates need an endpoint first.** Configure your webhook endpoint and signing secret in [**Settings → Developer Tools**](/settings/workspace-settings/developer-tools) before you turn any of these on.
</Note>

### Protect: Compliance & Delivery

| Template | What it does | Build reference |
| --- | --- | --- |
| **Catch Failed Texts** | Surfaces, reopens, and assigns any text that fails to deliver | [Catch every text that fails to deliver](#catch-every-text-that-fails-to-deliver) |
| **Flag Bad Numbers** | Labels numbers whose failures look permanent | [Branch on the reason a message failed](#branch-on-the-reason-a-message-failed) |
| **Retry Later Queue** | Labels temporary failures that are worth retrying | [Branch on the reason a message failed](#branch-on-the-reason-a-message-failed) |
| **Label and Close Opted-Out Conversations** | Labels an opt-out and closes the thread | [Opted-out conversation handler](#3-opted-out-conversation-handler) |
| **Sync Opt-Outs to Your CRM** | Pushes opt-out events downstream | [Opted-out conversation handler](#3-opted-out-conversation-handler) |
| **Archive Every Message (Retention)** | Streams inbound and outbound messages to your retention store | [Every field message is retained and auditable](#10-push-new-inbound-message-to-an-external-system) |
| **Assign a Compliance Owner on Opt-Out** | Puts every opt-out on a named owner to confirm you honored it | [Opted-out conversation handler](#3-opted-out-conversation-handler) |

<Note>
  **MessageDesk already blocks sending to opted-out contacts** at the workspace level. The compliance templates are for internal awareness, ownership, and audit trails, not for blocking the outbound message.
</Note>

The **Custom Relay** (a blank canvas) is also available. Use it to build from scratch when no template fits.

<Tip>
  **Labels aren't created for you.** Templates that reference a label (`Resolved`, `No Reply Needed`, `Reopened`, `Delivery failed`, and so on) don't create it. Create the label first, or change the name in the template to one you already use. See [Labels](/inbox/labels).
</Tip>

<Tip>
  **Don't see the template you need?** Click **Suggest a Relay Template** at the bottom of the Relay Templates page. It routes you to MessageDesk's feedback board, where you can submit template ideas.
</Tip>

---

---

The **Custom Relay** (a blank canvas) and **Send Webhook** starters are also available. Use Custom Relay to build from scratch, or Send Webhook to push events to an endpoint.

<Tip>
  **Don't see the template you need?** Click **Suggest a Relay Template** at the bottom of the Relay Templates page in the app. It routes you to MessageDesk's feedback board, where you can submit template ideas.
</Tip>

---

## Foundational templates

The outcomes below are the building blocks that most teams start from. Each entry lists the **trigger**, any **conditions**, the **actions**, and a short "Why it works." Adjust wording, labels, assignees, and scoping to fit your team.

<Note>
  **Confirmed vs. pending.** The Add Action menu can vary by trigger. The templates below cover only the combinations confirmed to appear in the app today. Three additional foundational templates (on **Conversation Opened**, **Comment Created**, and **Conversation Label Applied**) are staged for a follow-up once their action menus land.
</Note>

### Whoever answers a lead first owns the follow-up

| Field | Value |
| --- | --- |
| **Trigger** | Message Sent |
| **Conditions** | Is unassigned conversation |
| **Actions** | Assign to sender |

**Why it works:** The first teammate to reply gets assigned. The conversation is no longer unassigned, so later replies from others don't steal it. Follow-up never lands in an unclaimed pile. This is the outcome behind the shipped **Auto Assign to First Responder** starter.

---

### Clean reset and system sync every time a thread closes

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Closed |
| **Conditions** | Teammate Assigned (Any teammate), or scope by line or label |
| **Actions** | Remove all teammates from the conversation → Add labels to the conversation (`Resolved`) → Send webhook |

**Why it works:** Closing a conversation doesn't unassign anyone on its own, and a new inbound message reopens the thread with those assignees still attached. That's how a closed thread comes back owned by whoever handled it last, even when they're not the right owner now. This Relay resets assignment so the next message lands unassigned and your routing handles it fresh. The `Resolved` label keeps reporting clean, and the webhook pushes the close event to your CRM, warehouse, or ticketing tool. Gating on **Teammate Assigned** set to Any teammate means the Relay only runs when someone's actually assigned.

This extends the shipped **Unassign on Close** starter with a `Resolved` label and an outbound webhook.

---

### Auto-close conversations that don't need a reply

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Label name equals `No Reply Needed` |
| **Actions** | Close the conversation → Remove Label where name equals `No Reply Needed` |

**Why it works:** Courtesy replies like "thanks!" reopen threads that nobody needs to touch. Label a conversation `No Reply Needed` when you're done with it. The next inbound message closes the thread and clears the label, so a real follow-up later reopens it normally.

<Note>
  **Ships as a template:** This build is available as the **Auto-Close No-Reply-Needed** starter from the Relays module. MessageDesk doesn't create the label for you. Create a label named `No Reply Needed` before turning the Relay on, or change the name in both the condition and the action to a label you already use. See [Labels](/inbox/labels). The **Label name equals** condition and **Remove Label where name equals** action are documented in [Relay building blocks](/relays/building-blocks).
</Note>

---

### One rule that routes an entire namespace of labels

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Conversation label equals, operator **startsWith**, value `Region:` |
| **Actions** | Send webhook → Add labels to the conversation (`Routed`) |

**Why it works:** The **Conversation label equals** condition is a matcher, not a fixed equality check. A single `startsWith "Region:"` rule catches every label in a namespace at once (`Region:North`, `Region:South`, `Region:West`, etc.), so you avoid maintaining one Relay per region. See the operator-selector Note in [Relay building blocks](/relays/building-blocks) for the full operator list.

---

### Catch every text that fails to deliver

| Field | Value |
| --- | --- |
| **Trigger** | Message Failed |
| **Conditions** | None (or scope to a line with **My phone number equals**) |
| **Actions** | Add labels to the conversation (`Delivery failed`) → Open the conversation → Assign teammates to the conversation (an owner) → Send webhook (alert a channel) |

**Why it works:** A failed text is invisible today because the sender assumes it went through. This surfaces every failure immediately, reopens and flags the thread, and assigns follow-up. Most valuable for time-sensitive dispatch, staffing, and safety messages.

---

### Branch on the reason a message failed

| Field | Value |
| --- | --- |
| **Trigger** | Message Failed |
| **Conditions** | Message failure contains text (match the carrier error text for a temporary or unreachable failure) |
| **Actions** | Add labels to the conversation (`Retry later`) → Send webhook |

**Why it works:** Not all failures are equal, so matching on the failure text lets you retry what's worth retrying and clean up what isn't. Build a second Relay on the same trigger that matches a permanent-failure reason and labels it `Bad number` instead. Use provider-agnostic error wording in your matches so the Relay keeps working if the underlying carrier or provider changes.

<Note>
  **Verify before enabling.** The **Message failure contains text** conditions. If your workspace doesn't see it on the Message Failed trigger yet, hold this template until it appears.
</Note>

---

### Out-of-office for one teammate, even on a shared line

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Active hours (with **Trigger outside of business hours** enabled), and **Teammate Assigned** set to the teammate who's out. Optionally add **an "Is unassigned" conversation** with **OR** to cover brand-new threads. |
| **Actions** | Reply to message ("I'm out of the office and will follow up when I'm back. Reply STOP to opt out.") |

**Why it works:** The auto-reply fires only for the conversations owned by the person who's out. One teammate can be away without silencing the whole shared line.

---

### Cover several shift windows with one set of Relays

| Field | Value |
| --- | --- |
| **Conditions** | Active hours, set to one window, plus **Teammate Assigned** or a line condition if the window is person-specific |
| **Actions** | Assign teammates to the conversation → **Stop Processing** |

**Why it works:** Most teams don't have one open window and one closed window. They have the hours the admin works, the evenings one group covers, the weekend another group covers, and the hours nobody works. One Relay can't express that, but one Relay per window can.

Build a separate Relay for each window, each with its own **Active hours** and its own assignee. Then order them with the narrowest or highest-priority window first. Every matching Relay runs, and no Relay knows another already fired, so **Stop Processing** as the last action is what stops the later windows from also assigning. See [Order your Relays](/relays/create-edit#1a-order-your-relays).

<Tip>
  **Put the busiest window first.** If your admin covers weekday business hours, that Relay goes at the top with Stop Processing, so the evening and weekend Relays below it never fire during her shift. The unstaffed window is the one to build last: give it an auto-reply rather than an assignee, since there's nobody to route to.
</Tip>

---

### Leave an automatic handoff note for the team

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Users Assigned |
| **Conditions** | None (or scope to a line) |
| **Actions** | Send Comment (an internal note with context for the new owner, for example "Assigned by Relay. Customer texted after hours, please follow up first thing.") |

**Why it works:** **MessageDesk doesn't notify anyone when they're assigned a conversation.** Assignment is silent, so a thread can sit with a new owner who has no idea it's theirs. This Relay is how the handoff actually gets noticed: the comment carries the context, and @mentioning the new owner in it fires the **@Mention in a comment** notification they do receive. The contact never sees any of it.

Assigning more than one teammate works too, which is how a manager lets their team reply on their behalf. Mention each of them in the comment.

<Note>
  **Assignment isn't a notification event.** MessageDesk notifies on new messages, @mentions in comments, and finished exports and imports. Being assigned isn't on that list, and the **Only notify me about conversations assigned to me** email setting filters *new message* notifications to your threads rather than alerting you when you're assigned one. See [Notification preferences](/settings/my-settings/notification-preferences).
</Note>

---

### When a label comes off, sync the change

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Label Removed |
| **Conditions** | Conversation has label (or scope by line with **My phone number equals**) |
| **Actions** | Send webhook (update the downstream system) → Assign teammates to the conversation (as needed) → Add labels to the conversation (as needed) |

**Why it works:** A label coming off often signals a state change. Firing on removal keeps your CRM or reporting in sync automatically. **Remove labels isn't an available action on this trigger**, so this Relay reacts to a removal rather than performing one.

---

### Log and route inbound calls to the right owner

| Field | Value |
| --- | --- |
| **Trigger** | Call Received |
| **Conditions** | None (or **My phone number equals** to scope to a specific line) |
| **Actions** | Add labels to the conversation (`Inbound call`) → Assign teammates to the conversation (line owner or on-call) → Send Comment (context for the assignee) → Send webhook (notify Slack, Teams, or your CRM) |

**Why it works:** Voice activity now lives alongside SMS in the same conversation thread. Firing on the call itself gets an owner assigned, drops an internal note for context, and pings your notification tool the moment the phone rings, without waiting for a follow-up text.

<Note>
  **Call Received works only for fully ported (voice and text) and text-only numbers.** It doesn't fire for Twilio-connected numbers or hosted (text-enabled) numbers that are already tied to a voice service. **Reply to message** isn't available on this trigger. See [Call Received actions](/relays/building-blocks#actions-on-the-new-triggers) for the full action list.
</Note>

---

### Turn an event in another tool into a text

| Field | Value |
| --- | --- |
| **Trigger** | Inbound Webhook Received |
| **Conditions** | **Webhook Payload Field Equals** (optional, to branch on a payload value) |
| **Actions** | Send Message (choose the outbound channel, use **Event Data** from the payload for the text, and include "Reply STOP to opt out.") |

**Why it works:** Your CRM, form, or workflow tool can text a contact through MessageDesk the moment something happens, without anyone copying details by hand.

---

### Keep a contact property in sync from an outside system

| Field | Value |
| --- | --- |
| **Trigger** | Inbound Webhook Received |
| **Conditions** | None, or **Webhook Payload Field Equals** |
| **Actions** | Update Contact Property (set a property from the payload using **Event Data**) |

**Why it works:** When a value changes in another system, the webhook automatically updates the matching contact property in MessageDesk. **Text** properties accept any payload value. Non-text properties (for example, dropdown or date) map the matching Custom Property from the payload, and the value must pass [type validation](/relays/webhooks#custom-property-validation).

<Note>
  **Inbound Webhook Received is a non-conversation trigger.** It runs against the contact in the payload, so conversation-scoped conditions don't apply. **Add labels to the conversation** is available. See [Webhooks](/relays/webhooks#inbound-webhooks) for the endpoint URL, the `X-MD-Signature` header, and the payload schema.
</Note>

---

### Bridge a contact property to a label and an owner

**Contact Created** and **Contact Updated** fire against a contact rather than a conversation, so on their own they can't label or assign anything. That's a problem when the thing deciding ownership lives in a contact property: a `Clinician`, `Account owner`, or `Territory` field.

The way through is a comment. **Send Comment** creates the conversation and writes the property value into it. **Comment Created** is a conversation trigger, so it can label and assign. The comment is the bridge.

**Part 1: write the property into a comment.** Build this twice, once on each trigger.

| Field | Value |
| --- | --- |
| **Trigger** | Contact Created, then a second Relay on Contact Updated |
| **Conditions** | None, or **Contact Property Equals** to scope which contacts qualify |
| **Actions** | Send Comment, with **Event Data** set to the property that decides ownership |

**Part 2: read the comment, then label and assign.** Build one of these per possible value.

| Field | Value |
| --- | --- |
| **Trigger** | Comment Created |
| **Conditions** | Comment contains text, matching one property value (a person's name, a territory, an account tier) |
| **Actions** | Add labels to the conversation → Assign teammates to the conversation |

**Why it works:** Updating one contact property does the rest. The contact Relay drops a comment carrying the value, and the matching Comment Created Relay turns that value into a label and an owner. Nobody labels or assigns by hand, and the label keeps working as a scope for every other Relay you build.

<Warning>
  **This needs a saved contact.** Contacts and conversations are separate records in MessageDesk, which is what lets an [anonymous conversation](/contacts/anonymous-opt-out-blocked) exist as a bare phone number. The bridge reads a contact property, so a conversation with no contact behind it has nothing to route on. You can still label anonymous conversations by hand, or with a Message Received Relay, but not with this pattern. Get contacts saved and their properties populated first.
</Warning>

<Note>
  **Expect a comment every time the property changes.** The Part 1 Relay on **Contact Updated** fires on every update to that contact, so the internal comment reposts each time. That's what keeps the label correct if someone edits the field later, and the comments accumulate in the thread.

  If the noise outweighs that, turn off the **Contact Updated** half and keep only **Contact Created**. The cost: a label that gets removed later won't come back on its own, and any Relay scoped to that label quietly stops matching that conversation. Contacts are visible to your team; internal comments are not visible to the contact either way.
</Note>

<Note>
  **A Relay's comment does fire Comment Created.** It won't re-trigger the Relay that posted it, so this can't loop, but it does reach other Comment Created Relays. That's the whole mechanism. See [Comment Created](/relays/building-blocks#triggers).
</Note>

<Tip>
  **Part 2 costs one Relay per value.** Fourteen clinicians means fourteen Comment Created Relays, each matching one name. They're near-identical, so build one and duplicate it. Keep the matched values distinct enough that **Comment contains text** can't hit two at once: `Sam Rivera` beats `Sam` when you also have a Samir.
</Tip>

<Tip>
  **What the property and label unlock downstream.** Build a [saved filter](/contacts/saved-filters) per value and each owner can pull up their own client list, select all, and message them at once. And because a [custom role](/settings/workspace-settings/team-management#custom-roles-and-permissions) can scope someone's Inbox to specific labels, the label this Relay applies also feeds who can see the thread. Build the role by copying **Operator** and setting **Label inboxes** to Editor or Viewer on just that person's label, with No Access elsewhere. The default Operator role won't do it on its own: its label permissions govern editing labels, not restricting what shows up in the Inbox. One caveat that matters here, since this Relay assigns as well as labels: [assignment overrides label scope](/inbox/labels#faq), so an assigned teammate can see the thread even with No Access to its label.
</Tip>

---

---

## Vertical library

The templates below are grouped by industry and by common cross-industry use cases.

Every template uses the canonical trigger, condition, and action names. See [Relay building blocks](/relays/building-blocks) for the full reference.

---

### Construction and dispatch

#### No job stalls because one supervisor's phone went dark

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | My phone number equals (the shared main or dispatch line) |
| **Actions** | Assign teammates to the conversation (crew or job coordinator) → Add labels to the conversation (job or crew) |

**Why it works:** Inbound texts land in a shared Inbox with an owner and a job label. Threads stay covered when a supervisor is on-site or out.

#### Subcontractor and supplier texts never sit unclaimed

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Is unassigned conversation |
| **Actions** | Add labels to the conversation (`Needs owner`) → Send webhook |

**Why it works:** Anything that slips past routing gets flagged instead of aging in the queue.

---

### Logistics and trucking

#### Route every driver text to the right dispatcher, no shared handset required

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | My phone number equals (dispatch line). Optionally add Conversation label equals a region. |
| **Actions** | Add labels to the conversation (region or load type) → Assign teammates to the conversation (that region's dispatcher) |

**Why it works:** Every dispatcher sees every message in one Inbox, and texts self-sort by region. That replaces the single shared phone.

#### Off-duty driver messages still get an instant acknowledgment

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Active hours (with **Trigger outside of business hours** enabled) |
| **Actions** | Reply to message ("Dispatch is closed. Text URGENT for the on-call line. Reply STOP to opt out.") → Add labels to the conversation (`After-hours`) |

**Why it works:** An off-hours text gets an immediate acknowledgment and waits for dispatch in the morning.

#### Policy and route acknowledgments get tracked, not chased

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `ACK` |
| **Actions** | Add labels to the conversation (`Acknowledged`) → Send webhook |

**Why it works:** Pair a broadcast with a keyword reply, and MessageDesk labels and logs every acknowledgment automatically.

---

### Field operations

#### Safety and incident texts escalate the second they land

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `incident` |
| **Actions** | Reply to message ("Received. A supervisor is being notified now.") → Assign teammates to the conversation (safety lead) → Add labels to the conversation (`Incident`) → Send webhook (alert the safety channel) |

**Why it works:** A field emergency routes to the safety lead and alerts the team in one step.

#### Rig recruiting stops living on personal phones

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `available` |
| **Actions** | Assign teammates to the conversation (recruiter) → Add labels to the conversation (`Available`) → Send webhook (push to your ATS) |

**Why it works:** MessageDesk captures every candidate availability reply, assigns an owner, and pushes it to your system. Nobody gets called twice.

#### Every field message is retained and auditable

| Field | Value |
| --- | --- |
| **Trigger** | Message Received (build a matching Relay on Message Sent) |
| **Conditions** | None (or scope to field lines) |
| **Actions** | Send webhook (to your retention or audit store) |

**Why it works:** Inbound and outbound field messages stream to your system of record. MessageDesk facilitates 10DLC registration as your CSP and honors opt-outs at the workspace level.

---

### Staffing and recruiting

#### Every candidate reply gets claimed by a recruiter

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `yes` |
| **Actions** | Assign teammates to the conversation (staffing coordinator) → Add labels to the conversation (`Shift response`) → Send webhook (sync to your ATS or CRM) |

**Why it works:** After a shift-fill text, replies route to the coordinator. The fastest responder gets placed first, and MessageDesk logs the reply.

#### Applicant first-touch that never goes cold

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `apply` |
| **Actions** | Reply to message ("Thanks for your interest. Here is the quick application: [link]. Reply STOP to opt out.") → Assign teammates to the conversation (recruiter) → Add labels to the conversation (`Applicant`) |

**Why it works:** An instant reply with the application link plus an assigned recruiter keeps the pipeline warm.

#### Unsaved candidate numbers get named automatically

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Is anonymous conversation |
| **Actions** | Reply to message ("Thanks for reaching out. What is your first and last name?") → Add labels to the conversation (`Unverified`) |

**Why it works:** Capturing the name up front keeps the directory clean and every thread attributable.

---

### Professional services

#### Website-to-text intake lands in a shared, trackable inbox

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Is new conversation |
| **Actions** | Reply to message ("Thanks for reaching out. What is your name and a short note on what you need help with?") → Assign teammates to the conversation (intake coordinator) → Add labels to the conversation (`Intake`) → Send webhook (create the record in your CRM) |

**Why it works:** The first inbound text runs intake and creates a trackable record.

#### Billing questions route straight to AR

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `invoice` |
| **Actions** | Assign teammates to the conversation (billing) → Add labels to the conversation (`Billing`) |

**Why it works:** Money questions bypass the general queue.

---

### More industries and use cases

The patterns below cover common use cases across additional industries.

#### Automotive service: a shared department inbox

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | My phone number equals (the service department line) |
| **Actions** | Assign teammates to the conversation (advisor) → Add labels to the conversation (`Service`) |

**Why it works:** Status texts stop landing on one advisor's phone. Any advisor can pick up the thread, and replies can carry photos and invoice links.

<Tip>
  Pair this with an inverted **Active Hours** Relay on the same line that uses **Reply to Message** to acknowledge after-hours questions and set morning expectations.
</Tip>

#### Financial advisors: broadcast replies come back to a tracked inbox

| Field | Value |
| --- | --- |
| **Trigger** | Message Received (build a matching Relay on Message Sent) |
| **Conditions** | None (or scope to the advisor line) |
| **Actions** | Assign teammates to the conversation (the advisor) → Add labels to the conversation (`Client`) → Send webhook (to your recordkeeping system) |

**Why it works:** After a broadcast, replies land with an owner and both directions stream to your records.

#### Home services and emergency trades: turn an after-hours text into tomorrow's job

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Active hours (with **Trigger outside of business hours** enabled), My phone number equals (jobs or emergency line) |
| **Actions** | Reply to message ("We received your message and will follow up first thing. If this is an active emergency, call [number]. Reply STOP to opt out.") → Add labels to the conversation (`After-hours`) → Assign teammates to the conversation (on-call coordinator) |

**Why it works:** MessageDesk captures and routes the lead even when the office is dark.

---

### Support and sales

#### A texted keyword menu that routes itself

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Message contains text `support` (build parallel Relays for `pricing` and `sales`) |
| **Actions** | Reply to message (matching acknowledgment) → Assign teammates to the conversation (matching team) → Add labels to the conversation (matching topic) |

**Why it works:** One number handles support, pricing, and sales, each with its own reply and owner.

<Tip>
  Keep a keyword-free catch-all Relay ordered **last** so any message that doesn't match a keyword still gets a graceful reply and an owner. Instead of relying only on ordering, add the **Stop Processing** action to each matched Relay so a specific match stops the catch-all from also firing on the same message.
</Tip>

---

## Customer service

### 1. After-hours auto-reply

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Condition** | Active hours: set your business hours, then toggle **Trigger outside of business hours** to invert the window |
| **Actions** | **Reply to message:** "We'll get back to you at 8am. Reply STOP to opt out." → **Mark Conversation Unread** |

**Why this works:** Inverting the Active hours window means the Relay only fires when an inbound message lands **outside** your business hours, so customers always get an immediate acknowledgment. The STOP language keeps you compliant with carrier opt-out requirements. The **Mark Conversation Unread** action forces the thread back into the **Unread** filter after the auto-reply so a teammate still picks it up in the morning.

<Note>
  **This build matches the shipped After-Hours Auto-Reply (Keep Unread) starter.** Without the **Mark Conversation Unread** action, a Relay reply moves the conversation to **Replied** and out of the Unread filter. Including it forces the thread back to Unread so the customer's message is still queued for human review. See [Relays and conversation status](/relays/create-edit#relays-and-conversation-status).
</Note>

<Tip>
  Add a **Delay** to the Reply action to avoid sending repeat auto-replies inside a tight back-and-forth.
</Tip>

---

### 2. First-touch reply for anonymous contacts

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Is anonymous conversation = **true** |
| **Actions** | **Reply to message:** "Thanks for texting us! What's your first and last name so we know who to help? Reply STOP to opt out." → **Add labels to the conversation:** `Lead` |

**Why this works:** New inbound texts from unsaved numbers come in as **anonymous conversations**. A first-touch reply captures the contact's name while it's fresh, and the `Lead` label gives sales/intake a single filter to work from. See [Anonymous, opted out & blocked](/contacts/anonymous-opt-out-blocked) for how anonymous status is determined.

---

<Tip>
  **Once your contact list is complete, anonymous means new.** If every existing customer, client, or candidate already exists as a contact, then an anonymous conversation is by definition someone you haven't met. That makes **Is anonymous conversation** a reliable stand-in for "new lead" with no keyword to match and nothing for the sender to remember. Add **Mark Conversation Unread** so the thread stays queued after any auto-reply, and assign it to whoever owns intake.

  The reverse is also true: while contacts are still missing, this condition fires on regulars too. Finish importing before you rely on it.
</Tip>

---

### 3. Opted-out conversation handler

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | **Message contains text** = `STOP` (catches the opt-out keyword itself), or **Conversation label equals** `Opted Out` (routes on a label applied earlier in your workflow) |
| **Actions** | **Add labels to the conversation:** `Opted Out` → **Assign teammates to the conversation:** support queue |

**Why this works:** Match the STOP keyword on the inbound message itself, or route on an `Opted Out` label that a keyword Relay, a teammate, or an integration applied upstream. Labeling the thread keeps it visible to compliance and support. Routing it to your support queue ensures someone confirms you honored the customer's request. Once you apply `Opted Out`downstream filters, Relays can avoid touching that contact.

<Note>
  MessageDesk automatically blocks sending to **opted-out contacts** at the workspace level. This Relay is for **internal awareness and audit**, not for blocking the outbound message itself.
</Note>

<Note>
  **Prefer the dedicated trigger.** The **Opt Out Created** trigger fires on the opt-out event itself, so it also catches opt-outs that don't arrive as a STOP text. Build the same labeling and routing actions on it instead of keyword-matching. Message-sending actions aren't available on that trigger, and conversation actions run only on channel-specific opt-outs. See [Opt Out Created](/relays/building-blocks#actions-on-the-new-triggers).
</Note>

---

## Operations and routing

### 4. Phone line routing

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Condition** | **My phone number equals** = the specific connected phone line |
| **Actions** | **Assign teammates to the conversation:** designated teammate(s) → **Add labels to the conversation:** line-specific label (e.g., `Front Desk`, `Sales Line`) |

**Why this works:** Teams running multiple phone lines (Front Desk, Support, Sales) want every inbound message on that line to land with the right owner immediately. Adding a line-specific label also gives you ready-made filtered views in the Inbox.

---

### 5. Keyword-based routing

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Condition** | **Message contains text** = `HELP` (or `SUPPORT`, `BILLING`, etc.) |
| **Actions** | **Reply to message:** "Got it. A support teammate will be with you shortly." → **Assign teammates to the conversation:** support team |

**Why this works:** Keyword routing is fast triage: the customer gets an instant acknowledgment, and the right team picks up the thread without manual sorting. Combine with **My phone number equals** if you want the keyword to behave differently on different lines.

<Tip>
  Stack multiple keyword Relays (one per keyword) and use a default Relay without a keyword condition as a catch-all. Add the **Stop Processing** action to each keyword Relay so a specific match stops the catch-all from also firing on the same message, instead of relying only on ordering.
</Tip>

---

<Note>
  **Routing a keyword to a specific person multiplies.** A Relay matches one keyword and does one thing. It can't branch, so "route each of these five phrases to whichever of our fourteen people owns the thread" is 5 × 14 = **70 Relays**, one per pair. That's deliberate: Relays stay simple so they stay predictable.

  Three ways to keep it manageable. Pick one shared keyword like `urgent` and build one Relay per person, which is 14 instead of 70. Route the keyword to a single triage owner and let them reassign. Or move the branching to [Zapier](/integrations/zapier), which handles multi-condition logic in one Zap at the cost of a separate subscription.
</Note>

---

### 6. Auto-assign (round-robin note)

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | Optional: **My phone number equals** a specific line, or **Is unassigned conversation** |
| **Actions** | **Assign teammates to the conversation:** teammate |

**Why this works:** A single Relay assigns every inbound conversation to one teammate. **True round-robin distribution requires multiple Relays**, one per teammate, each segmented by a different **line** or **label** condition so they don't all fire on the same message. There isn't a built-in "rotate to next teammate" action today. This multi-Relay pattern is the supported workaround.

<Tip>
  Pair this with a `Triage` label, and an Inbox filter on **Assignee = Unassigned** so anything that slips through is easy to spot.
</Tip>

---

### 7. Assign unassigned conversations to whoever replies first

| Field | Value |
| --- | --- |
| **Trigger** | Message Sent |
| **Condition** | **Is unassigned conversation** = **true** |
| **Actions** | **Assign to sender** |

**Why this works:** When a shared Inbox has no owner on a thread, the first teammate to reply usually should own the follow-up. This Relay watches outbound messages, checks that no one's assigned yet, and assigns the conversation to whoever hit send. New inbound messages on the same thread then route to that teammate by default.

<Note>
  **Ships as a template:** This template is available as the **Auto Assign to First Responder** starter from the Relays module. Install it as-is or use the fields above to build it yourself.
</Note>

<Note>
  The **Assign to sender** action is only available with the **Message Sent** trigger.
</Note>

<Tip>
  Drop the **Is unassigned conversation** condition if you want the sender to always take ownership, even on threads someone else was already assigned to.
</Tip>

---

### 8. Notify the new assignee on assignment

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Users Assigned |
| **Conditions** | None (or scope by line/label) |
| **Actions** | **Send webhook** → your Slack/Teams/notification endpoint |

**Why this works:** MessageDesk's in-app notifications reach assignees inside the product. Teams that live in Slack or Teams want a ping in their chat tool the moment a conversation lands in their queue. Fire a webhook on **Conversation Users Assigned** and route the payload to your notifier of choice.

<Tip>
  Pair with **Add labels to the conversation** (for example, `Handoff`) so the receiving teammate can filter their Inbox to just newly assigned threads.
</Tip>

---

### 9. Re-triage on reopen

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Opened |
| **Conditions** | None (or scope by line/label) |
| **Actions** | **Remove all teammates from the conversation** → **Add labels to the conversation:** `Reopened` |

**Why this works:** When a closed conversation gets reopened (a customer replies days later, or a teammate reopens for follow-up), the original assignees may no longer be the right owners. Clearing assignees and applying a `Reopened` label sends the thread back through your normal triage so it gets picked up by the right person.

<Tip>
  Pair this with a **Message Received** Relay that auto-assigns based on line or keyword. Reopens get reset here; the next inbound gets routed there.
</Tip>

---

## Webhooks and integrations

<Note>
  Webhook actions require a configured webhook endpoint and signing secret. Set those up in [**Settings → Developer Tools**](/settings/workspace-settings/developer-tools) before turning on the Relays below. See [Send Webhook](/relays/webhooks#outbound-webhooks) for the full payload format, headers, and signature validation.
</Note>

### 10. Push new inbound message to an external system

| Field | Value |
| --- | --- |
| **Trigger** | Message Received |
| **Conditions** | None (or scope by line/keyword) |
| **Actions** | **Send webhook** → your configured endpoint |

**Why this works:** Streams every new inbound text to your CRM, data warehouse, ticketing tool, or notification service. Pair with **My phone number equals** to limit the firehose to a specific line, or add **Message contains text** to forward only certain keywords.

---

### 11. Notify an external system on conversation close

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Closed |
| **Conditions** | None (or scope by line/label) |
| **Actions** | **Send webhook** → your configured endpoint (plus any close-out actions you want to run in-app) |

**Why this works:** Use the **Conversation Closed** trigger directly. It fires whenever a teammate closes a conversation, so you can push the close event straight into a CRM, ticketing tool, or data warehouse. Add **My phone number equals** or **Conversation label equals** if you only want to forward closes from certain lines or queues. No label workaround needed.

---

### 12. Reset assignees on close

| Field | Value |
| --- | --- |
| **Trigger** | Conversation Closed |
| **Conditions** | **Teammate Assigned** (Any teammate) |
| **Actions** | **Remove all teammates from the conversation** |

**Why this works:** When a conversation closes, the previous assignees usually shouldn't stay attached. Reopens or new inbound messages on the same thread often need fresh triage. The **Teammate Assigned** condition (set to Any teammate) makes sure the Relay only runs when someone's actually assigned. The **Remove all teammates from the conversation** action clears every assignee in one step. The next inbound lands unassigned, and your normal auto-assign Relays can route it.

<Note>
  **Ships as a template:** This template is available as the **Unassign on Close** starter from the Relays module. Install it in one click, or build it from the fields above.
</Note>

<Tip>
  Pair this with a **Message Received** Relay that auto-assigns based on line or keyword. Closed threads get reset here; the next inbound gets routed there.
</Tip>

---

### 13. Auto-close after acknowledgment

| Field | Value |
| --- | --- |
| **Trigger** | Message Sent |
| **Conditions** | Optional: **My phone number equals** a specific line, or match an acknowledgment template/keyword |
| **Actions** | **Close the conversation** |

**Why this works:** One-shot confirmation flows (appointment confirmations, delivery notifications, "on my way" pings) don't need to stay open in the Inbox after the outbound message goes out. The **Close the conversation** action closes the thread as soon as the confirmation is sent, keeping the Inbox focused on conversations that still need a human.

<Tip>
  MessageDesk handles reopens automatically: a new inbound message on a closed conversation reopens the thread and fires any **Conversation Opened** Relays you've configured (see [Template 9](#9-re-triage-on-reopen)).
</Tip>

---

## See also

- [Relay building blocks](/relays/building-blocks): reference for all triggers, conditions, and actions
- [Create and edit a Relay](/relays/create-edit): step-by-step Relay building, including AND/OR conditions, delays, and active hours
- [Send Webhook](/relays/webhooks#outbound-webhooks): payload structure, signature validation, and retry behavior
- [Developer Tools](/settings/workspace-settings/developer-tools): configure your webhook endpoint and signing secret
- [Relays overview](/relays/overview): concepts and examples
